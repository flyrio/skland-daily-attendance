# API 手册

## 概述

本项目以“定时任务”形态运行签到逻辑，不以提供公共 HTTP API 为主要目标。

目前仅提供最小路由用于占位/健康检查。

---

## 接口列表

### routes

#### [GET] /
**描述:** 返回简单的 `Hello World` HTML 页面，用于验证部署是否可访问。

**请求参数:** 无

**响应:** `text/html`
